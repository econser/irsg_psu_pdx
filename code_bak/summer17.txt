start with partial scenes, full scenes if time permits

what image features give rise to low ranks for the ground truth image?
 - certain objects?
 - certain relationships?
 - 
what image features give rise to high ranks for the ground truth image?

check how object-level classification performance affects retreival performance
 - assume the object r-cnn is good at scoring cars, does it retrieve car scenes better than other scenes, i.e. is it primarily the r-cnn scoring that drives retreival performance
 - replace object scoring with the uniform scoring model for all objects and see how it changes
 
What happens when we query Man wearing red shirt vs woman wearing blue shirt





--------------------------------------------------------------------------------
what if we score each unrecognized object as one of the known classes, and do this for each recognized class?  We could then use the lowest energy as the object.
